# Alias definitions

# Utils
alias aptup='sudo apt update && sudo apt upgrade'
alias shutdn='shutdown now'

# Application

# Android Studio
alias android-studio='cd /opt/android-studio/bin/ && ./studio.sh'
